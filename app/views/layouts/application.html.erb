<!DOCTYPE html>
<html class="has-navbar-fixed-bottom">
  <head>
    <title>
      <% if content_for?(:title) %>
        <%= yield(:title) %>
      <% else %>
        Credit Card Balance Tracker
      <% end %>
    </title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>   
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'hamburger' %>    
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">    
  </head>  

  <body>
    <nav class="navbar is-dark is-inverted" role="navigation" aria-label="main navigation">  
      <div class="container">
        <div class="navbar-brand">
          <%= link_to icon("fas", "dollar-sign", "Credit Card Balance Tracker"), root_path, class: "navbar-item" %>        
        <div class="navbar-burger is-active" data-target="navMenu" aria-label="menu" aria-expanded="false">
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
        </div>
      </div>    

     <div id="navMenu" class="navbar-menu is-active">    
       <div class="navbar-start"> 
         <%= link_to icon("fas", "home", "Home"), root_path, class: "navbar-item" %>     
       </div>
      </div>
    </nav>

    <div class="container">      
      <% flash.each do |key, message| %>
        <div class="notification notification-<%= key %>">
          <%= message %>
        </div>     
        <% end %>
      <%= yield %>    
    </div>
    
    <nav class="navbar is-fixed-bottom">
      <div class="navbar-item footer-info">
        <p>
          <strong>Developed</strong> with <%= icon("fas", "heart") %>by Tim Fleming | aka: The <strong>GREAT</strong> One | Credit Card Balance Tracker &copy; 2020      
        </p>
      </div>
    </nav>    
  </body>
</html>


